import 'package:flutter/material.dart';

/// Приветственный экран на входе в приложение
class OnboardingScreen extends StatelessWidget {
  const OnboardingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
    );
  }
}
